# Supabase Configuration
# احصل على هذه القيم من: Settings → API في لوحة تحكم Supabase
# Get these values from: Settings → API in Supabase Dashboard

# Project URL - عنوان المشروع
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Anon Public Key - المفتاح العام
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key-here

# ملاحظات مهمة | Important Notes:
# 1. انسخ هذا الملف إلى .env واستبدل القيم بمفاتيحك الفعلية
#    Copy this file to .env and replace values with your actual keys
#
# 2. لا ترفع ملف .env إلى Git (محمي بواسطة .gitignore)
#    Do NOT commit .env to Git (protected by .gitignore)
#
# 3. استخدم anon key فقط (ليس service_role key)
#    Use anon key only (NOT service_role key)
#
# 4. راجع ملف GET_SUPABASE_KEYS.md للحصول على تعليمات مفصلة
#    See GET_SUPABASE_KEYS.md for detailed instructions
